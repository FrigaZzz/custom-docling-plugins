# Environment configuration for Docling Picture Description Plugin
# Copy this file to `.env` and fill in the values. Do NOT commit your `.env`.

# Azure OpenAI configuration (recommended)
# Example endpoint format: https://YOUR_RESOURCE_NAME.openai.azure.com
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
# The name of your Azure OpenAI deployment (e.g., gpt-4o-mini or custom)
AZURE_OPENAI_DEPLOYMENT=
# API version used for chat completions
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Alternative: OpenAI-compatible server (e.g., local vLLM, custom gateways)
# If AZURE_* vars are not set, the script falls back to these.
OPENAI_COMPATIBLE_API_URL=http://localhost:8000/v1/chat/completions
OPENAI_COMPATIBLE_API_KEY=
# Some providers use 'Authorization: Bearer ...'. Default header is 'api-key'.
OPENAI_COMPATIBLE_API_HEADER_NAME=api-key

# Plugin behavior configuration
# Prompt used for image description
PICTURE_DESCRIPTION_PROMPT=Describe the image in three sentences. Be concise and accurate.
# Request timeout in seconds
PICTURE_DESCRIPTION_TIMEOUT=90
# Key inside the response usage dict to extract token/response info (if supported by backend)
PICTURE_DESCRIPTION_TOKEN_EXTRACT_KEY=usage

# Input document to convert (local path or URL)
SOURCE_DOCUMENT=https://arxiv.org/pdf/2408.09869
